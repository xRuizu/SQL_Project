CREATE TABLE books (id INTEGER PRIMARY KEY, name TEXT, rating INTEGER);
INSERT INTO books VALUES (1, "Science:", 10);
INSERT INTO books VALUES (2, "Atomic Habits:", 10);
INSERT INTO books VALUES (3, "Science:", 10);

/* SELECT ALL ROWS AND COLUMNS in books */
SELECT * FROM books;
